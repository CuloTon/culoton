---
import Layout from '../../layouts/Layout.astro';
import TokenListings from '../../components/TokenListings.astro';
import type { Locale } from '../../i18n/strings';

export function getStaticPaths() {
  return [
    { params: { locale: 'ru' } },
    { params: { locale: 'pl' } },
    { params: { locale: 'de' } },
    { params: { locale: 'es' } },
    { params: { locale: 'uk' } },
  ];
}

const { locale } = Astro.params as { locale: Locale };

const META: Record<string, { title: string; description: string }> = {
  ru: { title: 'TAX — токены GRAM с графиком', description: 'Отобранные токены GRAM: график цены, капитализация и объём за 24ч, налог buy/sell/holders.' },
  pl: { title: 'TAX — tokeny GRAM z wykresem', description: 'Wyselekcjonowane tokeny GRAM: wykres ceny, kapitalizacja i obrót 24h, podatek buy/sell/holders.' },
  de: { title: 'TAX — GRAM-Token mit Chart', description: 'Ausgewählte GRAM-Token: Preis-Chart, Marktkapitalisierung und 24h-Volumen, Buy/Sell/Holders-Tax.' },
  es: { title: 'TAX — tokens GRAM con gráfico', description: 'Tokens GRAM seleccionados: gráfico de precio, capitalización y volumen 24h, tax de buy/sell/holders.' },
  uk: { title: 'TAX — токени GRAM з графіком', description: 'Відібрані токени GRAM: графік ціни, капіталізація та обсяг за 24г, податок buy/sell/holders.' },
};
const m = META[locale] ?? META.pl;
---

<Layout
  locale={locale}
  pathWithoutLocale="/token"
  title={m.title}
  description={m.description}
>
  <TokenListings locale={locale} />
</Layout>
