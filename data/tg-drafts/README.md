# Telegram drafts — pending bot unfreeze

Drafts here are written but NOT sent. The Telegram bot is frozen (see [[project_ton_news_site]] §"BOT FREEZE") until the user signals to unfreeze with the new $BRT contract address.

Once unfrozen, send these manually via Telegram, or extend `scripts/telegram_notify.py` with a `--mode editorial --file data/tg-drafts/<name>.txt` flag.

Format: plain UTF-8 text, MarkdownV2-safe. Each file = one post.
